<div id="<%= controller.controller_name %>_<%= controller.action_name %>">
	<h1 class="text-center">Blog Posts</h1>
	<hr class="post-hr"/>
	<div class="row">
		<% @posts.each do |post| %>
			<article class="col-sm-8 col-sm-offset-2 single-post">
				<header>
					<h1 class="text-center">
						<%= link_to post.title, post_path(post) %>
						<br>
						<small class="date"><%= post.created_at.strftime('%A, %B %d, %Y') %></small>
					</h1>
				</header>
				<div class="single-post-exerpt">
					<%= markdown(post.content) %>
				</div>
			</article>
		<% end %>
	</div>
	<div class="row">
		<%= will_paginate @posts %>
	</div>
</div>
